<% if (page.total > 1){ %>
    <nav class="pagination">
        <%- paginator({
            // prev_text: "&laquo; Prev",
            // next_text: "Next &raquo;"
            prev_next: true,
            end_size: 1,
            mid_size: 3,
        }) %>
    </nav>
<% } %>